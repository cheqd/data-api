export const TOKEN_EXPONENT: number = Number(process.env.TOKEN_EXPONENT);
export const TOKEN_DECIMALS = 10 ** TOKEN_EXPONENT;

export const REST_API = process.env.REST_API;
export const GRAPHQL_API = process.env.GRAPHQL_API;
